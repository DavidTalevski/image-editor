{
  "name": "web-image-processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm --prefix image-processor start\" \"npm --prefix image-editor start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Talevski",
  "license": "ISC",
  "workspaces": ["image-editor", "image-processor"],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}